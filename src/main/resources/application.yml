micronaut:
  application:
    name: fileService
netty:
  default:
    allocator:
      max-order: 3
app:
  root-dir: /tmp/test
  buffer-pool:
    enabled: false
    size: 1024
    buffer-size: 8192
  max-file-chunk-size: 1048576
  simple-allocator:
    enabled: true
    buffer-size: 8192